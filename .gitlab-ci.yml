# GitLab CI/CD Pipeline for Cline GUI
# Builds Electron app for Windows, macOS, and Linux
# Uses shell executor with docker run for containerized builds

stages:
  - test
  - build
  - release

variables:
  # Node.js version
  NODE_VERSION: "20"
  # Package registry for auto-updates
  PACKAGE_REGISTRY_URL: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic"
  # Disable git clean during checkout - we handle cleanup ourselves after fixing permissions
  GIT_CLEAN_FLAGS: none

# Common cleanup template - fixes root-owned files from docker
# Uses docker to run chown as root (since host doesn't have passwordless sudo)
.docker_cleanup:
  before_script:
    # Fix permissions from any previous failed runs (using docker to get root)
    - docker run --rm -v "$CI_PROJECT_DIR:/app" alpine chown -R $(id -u):$(id -g) /app || true
    # Now clean up old files (git clean was disabled via GIT_CLEAN_FLAGS)
    - git clean -ffdx || true
    # Show available disk space and memory for diagnostics
    - df -h "$CI_PROJECT_DIR" || true
    - free -h || true
  after_script:
    # Always fix permissions after job (even on failure)
    # NOTE: Do NOT remove out/out-online here - after_script runs BEFORE artifact upload
    # Old build artifacts are cleaned by git clean -ffdx in before_script of the next job
    - docker run --rm -v "$CI_PROJECT_DIR:/app" alpine chown -R $(id -u):$(id -g) /app || true

# =============================================================================
# TEST STAGE
# =============================================================================

lint:
  stage: test
  tags:
    - shell
  extends: .docker_cleanup
  script:
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        node:${NODE_VERSION} \
        sh -c "npm ci --prefer-offline && npm run lint"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG

test:
  stage: test
  tags:
    - shell
  extends: .docker_cleanup
  script:
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        node:${NODE_VERSION} \
        sh -c "npm ci --prefer-offline && npm run test"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG

typecheck:
  stage: test
  tags:
    - shell
  extends: .docker_cleanup
  script:
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        node:${NODE_VERSION} \
        sh -c "npm ci --prefer-offline && npm run typecheck"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG
  allow_failure: true  # Allow failure until all type issues are resolved

# =============================================================================
# BUILD STAGE
# =============================================================================

# Linux build template
.build_linux:
  stage: build
  tags:
    - shell
  extends: .docker_cleanup
  script:
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        electronuserland/builder:20 \
        bash -c "apt-get update && apt-get install -y dpkg fakeroot rpm && npm ci && npm run make -- --platform=linux"

# Linux build for tags (with artifacts for release)
build:linux:
  extends: .build_linux
  artifacts:
    name: "cline-gui-linux-${CI_COMMIT_REF_SLUG}"
    paths:
      - out/make/**/*.deb
      - out/make/**/*.rpm
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG

# Linux build for manual testing (no artifacts - too large for GitLab limit)
build:linux:manual:
  extends: .build_linux
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual
      allow_failure: true

# =============================================================================
# Windows Build Templates
# =============================================================================
# Two variants:
# - OFFLINE (full bundle): Node.js + Git bundled, larger download, instant install
# - ONLINE (light bundle): Downloads Node.js + Git during install, smaller download
#
# Auto-update always downloads the OFFLINE (full) bundle for reliability

# Windows OFFLINE build template (bundles Node.js + Git)
# Simple one-liner like the original working build, just with CLINE_ONLINE_BUILD env
.build_windows_offline:
  stage: build
  tags:
    - shell
  extends: .docker_cleanup
  script:
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        -e CLINE_ONLINE_BUILD=false \
        electronuserland/builder:wine-mono \
        bash -c "curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt-get install -y nodejs bzip2 && npm ci && ./scripts/download-node-windows.sh && ./scripts/download-git-bash-windows.sh && npm run make -- --platform=win32"

# Windows ONLINE build template (smaller, downloads deps during install)
.build_windows_online:
  stage: build
  tags:
    - shell
  extends: .docker_cleanup
  script:
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        -e CLINE_ONLINE_BUILD=true \
        electronuserland/builder:wine-mono \
        bash -c "curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt-get install -y nodejs bzip2 && npm ci && npm run make -- --platform=win32 && mkdir -p out-online && cp -r out/make/squirrel.windows out-online/"

# Windows OFFLINE build for tags (main installer for auto-updates)
build:windows:
  extends: .build_windows_offline
  resource_group: windows-build
  artifacts:
    name: "cline-gui-windows-offline-${CI_COMMIT_REF_SLUG}"
    paths:
      - out/make/squirrel.windows/x64/*.exe
      - out/make/squirrel.windows/x64/*.nupkg
      - out/make/squirrel.windows/x64/RELEASES
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG

# Windows ONLINE build for tags (smaller download option)
build:windows:online:
  extends: .build_windows_online
  resource_group: windows-build
  artifacts:
    name: "cline-gui-windows-online-${CI_COMMIT_REF_SLUG}"
    paths:
      - out-online/squirrel.windows/x64/*.exe
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG

# Windows OFFLINE build for manual testing
build:windows:manual:
  extends: .build_windows_offline
  resource_group: windows-build
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual
      allow_failure: true

# Windows ONLINE build for manual testing
build:windows:online:manual:
  extends: .build_windows_online
  resource_group: windows-build
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual
      allow_failure: true

# macOS build requires macOS runner (self-hosted or cloud)
# Uncomment when macOS runner is available
# build:macos:
#   stage: build
#   tags:
#     - macos
#   script:
#     - npm ci
#     - npm run make -- --platform=darwin
#   artifacts:
#     name: "cline-gui-macos-${CI_COMMIT_REF_SLUG}"
#     paths:
#       - out/make/**/*.dmg
#       - out/make/**/*.zip
#     expire_in: 1 week
#   rules:
#     - if: $CI_COMMIT_TAG
#     - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
#       when: manual
#       allow_failure: true

# =============================================================================
# RELEASE STAGE
# =============================================================================

# Upload artifacts to GitLab Package Registry for auto-updates
# Publishes both OFFLINE (for auto-update) and ONLINE (for initial download) Windows builds
publish:packages:
  stage: release
  tags:
    - shell
  needs:
    - job: build:linux
      artifacts: true
    - job: build:windows
      artifacts: true
    - job: build:windows:online
      artifacts: true
  script:
    - |
      # Get version from tag
      VERSION=${CI_COMMIT_TAG#v}

      echo "Publishing version ${VERSION} to package registry..."

      # Initialize release info file
      mkdir -p release-info
      echo "TAG_VERSION=${VERSION}" > release-info/urls.env

      # Upload Linux DEB
      for file in out/make/deb/x64/*.deb; do
        if [ -f "$file" ]; then
          DEB_NAME=$(basename "$file")
          DEB_URL="${PACKAGE_REGISTRY_URL}/releases/${VERSION}/${DEB_NAME}"
          echo "Uploading $file..."
          curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
               --upload-file "$file" \
               "${DEB_URL}"
          echo "DEB_NAME=${DEB_NAME}" >> release-info/urls.env
          echo "DEB_URL=${DEB_URL}" >> release-info/urls.env
        fi
      done

      # Upload Linux RPM
      for file in out/make/rpm/x64/*.rpm; do
        if [ -f "$file" ]; then
          RPM_NAME=$(basename "$file")
          RPM_URL="${PACKAGE_REGISTRY_URL}/releases/${VERSION}/${RPM_NAME}"
          echo "Uploading $file..."
          curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
               --upload-file "$file" \
               "${RPM_URL}"
          echo "RPM_NAME=${RPM_NAME}" >> release-info/urls.env
          echo "RPM_URL=${RPM_URL}" >> release-info/urls.env
        fi
      done

      # Upload Windows OFFLINE installer (replace spaces in filename with dashes for URL)
      # This is the main installer used for auto-updates
      for file in out/make/squirrel.windows/x64/*.exe; do
        if [ -f "$file" ]; then
          WIN_NAME=$(basename "$file" | tr ' ' '-')
          WIN_URL="${PACKAGE_REGISTRY_URL}/releases/${VERSION}/${WIN_NAME}"
          echo "Uploading OFFLINE installer: $file as ${WIN_NAME}..."
          curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
               --upload-file "$file" \
               "${WIN_URL}"
          echo "WIN_NAME=${WIN_NAME}" >> release-info/urls.env
          echo "WIN_URL=${WIN_URL}" >> release-info/urls.env

          # Also upload with -offline suffix for clarity in release notes
          WIN_OFFLINE_NAME="${WIN_NAME%.exe}-offline.exe"
          if [ "${WIN_OFFLINE_NAME}" != "${WIN_NAME}" ]; then
            WIN_OFFLINE_URL="${PACKAGE_REGISTRY_URL}/releases/${VERSION}/${WIN_OFFLINE_NAME}"
            echo "Uploading as ${WIN_OFFLINE_NAME}..."
            curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
                 --upload-file "$file" \
                 "${WIN_OFFLINE_URL}"
            echo "WIN_OFFLINE_NAME=${WIN_OFFLINE_NAME}" >> release-info/urls.env
            echo "WIN_OFFLINE_URL=${WIN_OFFLINE_URL}" >> release-info/urls.env
          fi
        fi
      done

      # Upload Windows ONLINE installer (from build:windows:online job)
      # Stored in out-online/ to avoid conflicts with offline build
      # Smaller download, downloads Node.js + Git during installation
      for file in out-online/squirrel.windows/x64/*.exe; do
        if [ -f "$file" ]; then
          WIN_ONLINE_NAME=$(basename "$file" | tr ' ' '-' | sed 's/\.exe$/-online.exe/')
          WIN_ONLINE_URL="${PACKAGE_REGISTRY_URL}/releases/${VERSION}/${WIN_ONLINE_NAME}"
          echo "Uploading ONLINE installer: $file as ${WIN_ONLINE_NAME}..."
          curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
               --upload-file "$file" \
               "${WIN_ONLINE_URL}"
          echo "WIN_ONLINE_NAME=${WIN_ONLINE_NAME}" >> release-info/urls.env
          echo "WIN_ONLINE_URL=${WIN_ONLINE_URL}" >> release-info/urls.env
        fi
      done

      # Create latest.yml for auto-updater (electron-updater expects this at the root)
      # NOTE: path should NOT include version - the feed URL already points to version folder
      # Auto-updater always uses the OFFLINE (full) bundle for reliability
      echo "Creating latest.yml for auto-updater..."
      WIN_EXE=$(ls out/make/squirrel.windows/x64/*.exe 2>/dev/null | head -1)
      WIN_FILENAME=$(basename "$WIN_EXE" | tr ' ' '-')
      WIN_SHA512=$(sha512sum "$WIN_EXE" 2>/dev/null | cut -d' ' -f1 || echo "")
      RELEASE_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
      printf 'version: %s\npath: %s\nsha512: %s\nreleaseDate: %s\n' \
        "${VERSION}" "${WIN_FILENAME}" "${WIN_SHA512}" "${RELEASE_DATE}" > latest.yml

      # Upload latest.yml to the version-specific location in Package Registry
      echo "Uploading latest.yml to ${PACKAGE_REGISTRY_URL}/releases/${VERSION}/latest.yml"
      curl --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
           --upload-file latest.yml \
           "${PACKAGE_REGISTRY_URL}/releases/${VERSION}/latest.yml"

      # Copy latest.yml to release-info for the update-info job
      cp latest.yml release-info/latest.yml

      echo "Packages published successfully!"
      cat release-info/urls.env
  artifacts:
    paths:
      - release-info/
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/

# Publish update metadata for electron-updater via Job Artifacts API
# This job creates artifacts that can be accessed via:
# ${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/jobs/artifacts/${CI_COMMIT_TAG}/raw/latest.yml?job=publish:update-info
publish:update-info:
  stage: release
  tags:
    - shell
  needs:
    - job: publish:packages
      artifacts: true
  script:
    - |
      echo "Publishing update info for electron-updater..."
      cat release-info/latest.yml

      # Create update directory structure for electron-updater
      mkdir -p update-info
      cp release-info/latest.yml update-info/latest.yml

      echo "Update info published. Access via Job Artifacts API:"
      echo "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/jobs/artifacts/${CI_COMMIT_TAG}/raw/update-info/latest.yml?job=publish:update-info"
  artifacts:
    paths:
      - update-info/latest.yml
    expire_in: never
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/

# Create GitLab Release using release-cli
create:release:
  stage: release
  tags:
    - shell
  needs:
    - job: publish:packages
      artifacts: true
  script:
    - |
      TAG=${CI_COMMIT_TAG}
      echo "Creating release for ${TAG}"

      # Load URLs from publish:packages artifact
      source release-info/urls.env

      # Create description file with actual URLs
      cat > /tmp/release_description.md << DESCEOF
      ## Cline GUI ${TAG}

      ### Downloads

      #### Linux
      - **DEB (Debian/Ubuntu)**: [${DEB_NAME}](${DEB_URL})
      - **RPM (Fedora/RHEL)**: [${RPM_NAME}](${RPM_URL})

      #### Windows
      - **Full Installer (Recommended)**: [${WIN_NAME}](${WIN_URL})
        - Includes Node.js and Git - ready to use immediately
      - **Online Installer (Smaller)**: [${WIN_ONLINE_NAME}](${WIN_ONLINE_URL})
        - Downloads Node.js and Git during installation (~150MB)
        - Smaller initial download, longer installation time

      ### Notes
      - Auto-updates always use the full (offline) installer for reliability
      - Windows 10 or later required

      ---
      *Built automatically by GitLab CI/CD*
      DESCEOF

      cat /tmp/release_description.md

      docker run --rm \
        -e CI_JOB_TOKEN \
        -e CI_SERVER_URL \
        -e CI_PROJECT_ID \
        -v /tmp/release_description.md:/work/description.md:ro \
        -w /work \
        registry.gitlab.com/gitlab-org/release-cli:latest release-cli create \
          --name "Cline GUI ${TAG}" \
          --tag-name "${TAG}" \
          --description description.md
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
